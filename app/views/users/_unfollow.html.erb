<%= form_with(model: @user, url: relationship_path, method: :delete, local: true) do |form| %>
  <%= hidden_field_tag :followed_id, @user.id %>
  <div class="actions">
    <%= form.submit t("views.relationship.unfollow") %>
  </div>
<% end %>
