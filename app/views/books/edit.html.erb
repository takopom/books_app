<h1><%= t("views.editing_book") %></h1>

<%= render 'form', book: @book %>

<p>
  <%= button_to t("views.destroy"), @book, method: :delete, data: { confirm: t("messages.confirm") } %>
</p>

<%= link_to t("views.show"), @book %> |
<%= link_to t("views.back"), books_path %>
